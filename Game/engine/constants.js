/**
 * The map of direction constants to their respective strings
 * @readonly
 * @enum {string}
 */
export const DIRECTIONS = Object.freeze({
    UP: "up ",
    DOWN: "down ",
    LEFT: "left ",
    RIGHT: "right ",
});

export const constants = { DIRECTIONS };